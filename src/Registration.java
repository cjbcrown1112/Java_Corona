import java.util.logging.Logger;

public class Registration {

    final private static Logger logger = Logger.getLogger(Registration.class.getName());

    public void sendRegister(SMS sms) {
        sms.sms_checker(sms.get)
    }
}
